name: Push

on:
  push:
    branches:
      - main

jobs:
  get-latest-tag:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Get latest tag
        id: get_tag
        run: echo "LATEST_TAG=$(git describe --tags $(git rev-parse HEAD))" >> $GITHUB_ENV

      - name: Output latest tag
        run: echo "The latest tag is ${{ env.LATEST_TAG }}"

